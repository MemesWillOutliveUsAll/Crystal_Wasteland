add_namespace = mod_China
# Death to commie China!
country_event = {
	id = mod_China.1
	title = mod_China.1.t
	desc = mod_China.1.d
	picture = GFX_report_event_generic_sign_treaty3

	is_triggered_only = yes
	
	mean_time_to_happen = { days = 2 }
	
	# Accept
	option = {
	add_to_faction = MEN
	add_to_faction = MAN
	hidden_effect = {
	news_event = { id = news.32 }
		name = mod_China.1.a
		PRC = {
		load_oob = "PRC_revolt"
		declare_war_on = { type = annex_everything    target = CHI }
		declare_war_on = { type = annex_everything    target = YUN }
		declare_war_on = { type = annex_everything    target = SHX }
		declare_war_on = { type = annex_everything    target = GXC }
		declare_war_on = { type = annex_everything    target = MAN }
		declare_war_on = { type = annex_everything    target = MEN }
		}
		PRC = {
		transfer_state = 608
		transfer_state = 609
		transfer_state = 617
		transfer_state = 748
		transfer_state = 603
		
			annex_country = {
				target = SIK
				transfer_troops = YES
			}
		
		}
		
	
	}
	
	}
}

country_event = { # ultimatum
	id = mod_China.2
	title = mod_China.2.t
	desc = mod_China.2.d
	picture = GFX_report_event_generic_sign_treaty3

	is_triggered_only = yes
	
	mean_time_to_happen = { days = 2 }
	
	# Accept
	option = {
	random_list = {
			60 = {
				CHI = {
					annex_country = {
							target = ROOT
							transfer_troops = yes
					}
				}
			}
			40 = {
				declare_war_on = {
				target = CHI
				type = annex_everything
				}
			}
		}
	
	}
}


country_event = { # WIN
	id = mod_China.3
	title = mod_China.3.t
	desc = mod_China.3.d
	picture = GFX_report_event_generic_sign_treaty3

	is_triggered_only = yes
	
	mean_time_to_happen = { days = 2 }
	
	# Accept
	option = {
	transfer_state = 330
	}
}

country_event = { # LOSE
	id = mod_China.4
	title = mod_China.4.t
	desc = mod_China.4.d
	picture = GFX_report_event_generic_sign_treaty3

	is_triggered_only = yes
	
	mean_time_to_happen = { days = 2 }
	
	# Accept
	option = {
	add_stability = -0.5
	}
}

